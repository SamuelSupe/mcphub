# Console screenshots / 控制台截图

These are actual local Chrome captures of MCPHub v2.1.0 (`1539963`), taken on 2026-09-26. The server and example MCP services ran in an isolated OrbStack environment, with SQLite and a development Vault instance. The browser viewport was 1512 × 800; overview and tool-permission images include the full page.

截图来自本机 Chrome 中实际运行的 MCPHub v2.1.0（`1539963`），拍摄于 2026-09-26。服务端与示例 MCP 服务运行在独立的 OrbStack 环境，使用 SQLite 和开发模式 Vault。浏览器视口为 1512 × 800，概览与工具权限截图包含完整页面。

| Screen / 页面 | English | 中文 |
| --- | --- | --- |
| Service overview / 服务概览 | [View](overview.en.jpg) | [查看](overview.zh-CN.jpg) |
| Write tool permissions / 写工具权限 | [View](tool-policies.en.jpg) | [查看](tool-policies.zh-CN.jpg) |
| Vault personal accounts / Vault 个人账号 | [View](vault-accounts.en.jpg) | [查看](vault-accounts.zh-CN.jpg) |
| HTTP tool groups / HTTP 工具组 | [View](http-tools.en.jpg) | [查看](http-tools.zh-CN.jpg) |

All service names, tools, scopes, resource values and domains are demonstration data. The `github` backend is a local example MCP service, not a connection to GitHub. No real user account, enterprise identity provider, external API or credential is shown. The Vault configuration screenshot does not demonstrate a completed upstream OAuth login. Local administration does not expose the remote approval workflow.

所有服务名、工具、Scope、资源值和域名均为演示数据。名为 `github` 的后端是本地示例 MCP 服务，不表示已连接 GitHub。截图不包含真实用户、企业身份服务、外部 API 或凭证。Vault 配置截图不代表已完成上游 OAuth 登录；本地管理模式不展示远程审批流程。

To refresh these images, use the current production UI with an isolated sample configuration, keep this provenance note up to date, and verify both README galleries in GitHub after publishing.

更新截图时，请使用当前产品界面与独立示例配置，同步更新此说明，并在发布后检查 GitHub 中的两份 README 图集。
